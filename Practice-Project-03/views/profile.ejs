<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Page</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="bg-zinc-900 p-10 w-full min-h-screen text-white">
        <a href="/logout" class="bg-red-500 px-3 py-2 text-sm inline-block mb-5  font-bold">LogOut</a>
        <h1 class="text-3xl mb-5">Welcome, <%= user.name%>👋</h1>
        <h5 class="text-xl mb-5 ">You Can Create a Post.</h5>
        <form action="/post" method="post">
            <textarea name="content" class="resize-none bg-transparent border-2 border-zinc-800 rounded-md min-w-1/3 p-3 outline-none block" placeholder="What's In Your Mind🧠...." ></textarea>
            <input type="submit" value="Create New Post" class="px-5 py-2 mt-2 bg-blue-500 rounded-md block">
        </form>

        <div class="posts mt-10">
            <h3 class="text-zinc-400 font-bold text-xl mb-5">Your Posts</h3>
            <div class="postcontainer gap-4 flex flex-wrap ">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post min-w-1/3 p-4 border border-[1px] border-zinc-700 mb-4 rounded-md bg-zinc-800">
                        <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                        <p class="text-l tracking-thight"><%= post.content %></p>
                        <small class="inline-block mt-4"><%= post.likes.length %> Likes</small>
                        <div class="btns flex gap-4 mt-2">
                            <a href="/like/<%= post._id %>" class="text-blue-500">
                                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                            </a>
                            <a href="/edit/<%= post._id %>" class="text-zinc-500">edit</a>
                        </div>
                    </div>
                <% }) %>

                 
                
            </div>
        </div>

    </div>


</body>

</html>
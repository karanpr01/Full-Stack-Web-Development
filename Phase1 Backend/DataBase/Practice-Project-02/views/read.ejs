<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Read</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
     <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="nav mb-10">
            <a  class="text-blue-500 hover:underline" href="/">Back to Home</a>
        </div>


        <h1 class="text-3xl tracking-thighter mb-5 font-bold underline">All Users</h1>

        <div class="users flex gap-3 flex-wrap">
            <% if(users.length > 0){ %>
                <% users.forEach(function(user){ %>
                   <div class="user w-72 p-3 bg-zinc-700 rounded-lg">
                        <div class="w-full h-72 rounded-lg bg-zinc-500 overflow-hidden">
                            <img class="w-full h-full object-cover object-center" src="<%= user.imgUrl %>" alt="">
                        </div>
                        <h3 class="text-2xl tracking-thighter mt-3"><%= user.name  %></h3>
                        <h5 class="text-zinc-500"><%= user.email %></h5>
                        <div class="flex gap-5 mt-10">
                            <a class="text-blue-500 hover:underline" href="/edit/<%= user._id %>">Edit User</a>
                            <a class="text-red-500 hover:underline" href="/delete/<%= user._id %>">Delete User</a>
                        </div>
                    </div>

                <% }) %>

            <%} else { %>
                <h3 class="text-3xl underline text-zinc-500">No Users Yet🥲</h3>
                <%} %>
        </div>

    </div>
    
</body>
</html>